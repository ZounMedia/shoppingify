import { FC } from "react";
import styles from "@styles/ShoppingListFooter.module.scss";

const ShoppingListFooter: FC = () => {
  return <div className={styles.container}>ShoppingListFooter</div>;
};

export default ShoppingListFooter;
